#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


    

int main() {
    int N;

    
    scanf("%d", &N);

    int elements[N];

    
    
    for (int i = 0; i < N; i++) {
        scanf("%d", &elements[i]);
    }

    
    for (int i = 0; i < N; i++) {
        printf("%d ", elements[i]);
    }
    printf("\n");

    return 0;
}
